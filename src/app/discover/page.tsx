// Discover — "moves in my area" (Phase 13)
// Mockup: mooves-phase13-discover.html

import { Suspense } from 'react'
import DiscoverScreen from '@/components/discover/DiscoverScreen'

export default function DiscoverPage() {
  return (
    <Suspense>
      <DiscoverScreen />
    </Suspense>
  )
}
